const port = 3000

/* 127.0.0.1 */
const host = 'localhost'

/* 注册api目录 */
const apisDir = [ 'apis' ]

/* 跨域url配置 */
const originUrl = 'http://localhost:9999'

module.exports = {
    port,
    host,
    apisDir,
    originUrl
}